
export enum Action {

    JOINED,
    LEFT,
    LOGIN,
    RENAME,
    CONNECTED,
    DISCONNECT,
    MESSAGE,
    ERROR
}

export enum Event {
    CONNECT = 'connect',
    DISCONNECT = 'disconnect'
}
